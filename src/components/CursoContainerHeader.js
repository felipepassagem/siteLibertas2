import React from "react";
import CursoContainer from "./CursoContainer";

function CursoContainerHeader({ area }) {
  console.log(area[0])
  const cursos = area.shift();
  return (
    <section>
      
    </section>
  );
}

export default CursoContainerHeader;
